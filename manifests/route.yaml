apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: flask-gpu-demo
  namespace: flask-gpu-demo
spec:
  to:
    kind: Service
    name: flask-gpu-demo
  port:
    targetPort: 80
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
